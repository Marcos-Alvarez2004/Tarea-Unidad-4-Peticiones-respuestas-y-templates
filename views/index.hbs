<div class="contenedor">
  <h1>{{title}}</h1>
  {{#if conocido}}
    <p>Hola
      <b class="color">{{nombre}}</b>, ¿Como estas?
      <a href="/salir">Salir</a></p>
  {{/if}}

  {{#unless conocido}}
    <p>Porfavor ingrese su nombre de usuario</p>
    <form action="/ingresar" method="post">
      <input type="text" name="nombre" />
      <button>Enviar</button>
    </form>
  {{/unless}}
</div>